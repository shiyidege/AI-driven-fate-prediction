streamlit>=1.30.0
openai>=1.0.0
pydantic>=2.0.0
# 新增缺失的依赖（解决 langchain_chroma 导入失败）
langchain>=0.1.0
langchain-chroma>=0.1.0
chromadb>=0.4.0
langchain-community>=0.0.20  # 若 rag_chain 中用到 langchain 其他模块，需补充
